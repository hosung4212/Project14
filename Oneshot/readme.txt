stdout=int(r.recvline()[:-1],16) 항상 드는 생각인데 나는 파이썬 문법에 좀 약하다. 파이썬 공부 좀 해야할 듯
일단 주소 받는 코드를 보면 r.rcvline에서는 마지막 개행문자를 받는데 이걸 제거해주려고 [:-1]을 하고 있다. 그리고 16진수로 받은 후에 이를 int로 바꾼다. 
이걸 p64하면 주소임
이번에는 여태 해왔던것들이랑 비슷하다. 먼저 stdout의 주소를 출력해주기 때문에 stdout의 주소를 받는다. 그 후 원래 offset을 이용해 라이브러리의 베이스주소를 알아내고 onegadget의  오프셋을 이용해서 onegadget의 주소를 알아낸다. 이를 이용해서 ret의 주소를 onegadget으로 덮어쓰면 완성이다. 
onegadget은 one_gadget [파일이름] 으로 오프셋을 알아낼 수 있다.
